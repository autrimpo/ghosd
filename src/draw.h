#ifndef GHOSD_DRAW_H
#define GHOSD_DRAW_H

#include "common.h"

void draw_pos(struct config *cfg);
void draw_size(struct config *cfg);
void draw_clear(struct config *cfg);
void draw_bg(struct config *cfg);
void draw_body(struct config *cfg);

#endif
